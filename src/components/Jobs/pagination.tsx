import * as React from 'react';

type Props = {
  color?: string;
};

export const Pagination: React.FC<Props> = () => {
  return (
    <section>
      <h1>Pagination Component</h1>
    </section>
  );
};
